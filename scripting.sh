 e: filename == switch to the diffrent file 
    bn: == back to the previous opened file
       :vsp == split the vim into two windows
          ctrl+www == swithc the splitted windows by :vsp
	     j == for down 
	        k == for up
		   h == left
		      l == right
